using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HEllo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()

    {
        Debug.Log("hello, ı'm your new PET");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
